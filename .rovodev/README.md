# RovoDev Integration

AI-SDLC installs Spec Kit skills under `.rovodev/skills/` and prompt wrappers under `.rovodev/prompts/`.

RovoDev uses the Atlassian CLI outside the project. Any CLI installation or login is a manual user setup step and is not performed by `aisdlc init`.
