# CI / CD test pipeline

## Current Status
- There are mutiple spring boot microservices with basic tests
- There is a React web UI with no tests
- The project is stored on Github with no pipelines

## Requirements
- Expand the spring boot tests so that the functionality is tested meaningfully
- Add tests to the React UI
- Tests need to run when code is pushed to the repo
- Add a code coverage report
