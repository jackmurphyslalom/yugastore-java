# Architecture Assessment: yugastore-java

Generated by `rabbit-architecture-assessment-rollup` from the 7 per-tier assessments below. This
rollup only reads those files and never performs its own 16-factor scoring (contract:
`specs/003-architecture-assessment/contracts/rabbit-architecture-assessment-rollup.md`).

**Assessed**: 2026-09-15

## Tier Assessments

- [`eureka-server-local`](./eureka-server-local.md)
- [`products-microservice`](./products-microservice.md)
- [`checkout-microservice`](./checkout-microservice.md)
- [`cart-microservice`](./cart-microservice.md)
- [`api-gateway-microservice`](./api-gateway-microservice.md)
- [`login-microservice`](./login-microservice.md)
- [`react-ui`](./react-ui.md)

## C1: System Context

```mermaid
C4Context
  title System Context - yugastore-java

  Person(customer, "Customer", "Shops in the storefront via a browser")

  Enterprise_Boundary(b0, "yugastore-java") {
    System(reactUi, "react-ui", "React storefront UI; the only user-facing entry point")
    System(apiGateway, "api-gateway-microservice", "Single external API surface; fans out to downstream microservices")
    System(products, "products-microservice", "Product catalog and rankings")
    System(cart, "cart-microservice", "Shopping cart")
    System(checkout, "checkout-microservice", "Checkout, inventory, and order placement")
    System(login, "login-microservice", "User/Role auth - WIP, not wired to api-gateway")
    System(eureka, "eureka-server-local", "Service registry")
  }

  SystemDb_Ext(yugabyte, "YugabyteDB", "YCQL (products/checkout/orders/inventory) + YSQL (cart)")

  Rel(customer, reactUi, "Uses", "HTTPS")
  Rel(reactUi, apiGateway, "Calls", "HTTP")
  Rel(apiGateway, products, "Calls", "HTTP, via Eureka")
  Rel(apiGateway, cart, "Calls", "HTTP, via Eureka")
  Rel(apiGateway, checkout, "Calls", "HTTP, via Eureka")
  Rel(checkout, cart, "Calls", "HTTP, via Eureka")
  Rel(checkout, products, "Calls", "HTTP, via Eureka")
  Rel(products, yugabyte, "Reads/Writes", "YCQL")
  Rel(checkout, yugabyte, "Reads/Writes", "YCQL")
  Rel(cart, yugabyte, "Reads/Writes", "YSQL")
  BiRel(eureka, apiGateway, "Registers/discovers")
  BiRel(eureka, products, "Registers/discovers")
  BiRel(eureka, cart, "Registers/discovers")
  BiRel(eureka, checkout, "Registers/discovers")
  BiRel(eureka, login, "Registers/discovers")
```

## C2: Container

```mermaid
C4Container
  title Container Diagram - yugastore-java

  Person(customer, "Customer", "Shops in the storefront via a browser")

  System_Boundary(s0, "yugastore-java") {
    Container(reactUi, "react-ui", "React 16 / Create React App", "Storefront UI; proxies all calls to api-gateway on port 8081")
    Container(apiGateway, "api-gateway-microservice", "Spring Boot 2.6.3, port 8081", "Single external API surface")
    Container(products, "products-microservice", "Spring Boot 2.6.3, port 8082", "Product catalog + rankings")
    Container(cart, "cart-microservice", "Spring Boot 2.6.3, port 8083", "Shopping cart")
    Container(checkout, "checkout-microservice", "Spring Boot 2.6.3, port 8086", "Checkout + inventory + orders")
    Container(login, "login-microservice", "Spring Boot 2.6.3, port 8085", "User/Role auth - WIP, not wired to api-gateway")
    Container(eureka, "eureka-server-local", "Spring Cloud Netflix Eureka, port 8761", "Service registry")
  }

  ContainerDb_Ext(ycql, "YugabyteDB (YCQL)", "Cassandra-compatible", "products/checkout/orders/inventory data")
  ContainerDb_Ext(ysql, "YugabyteDB (YSQL)", "Postgres-compatible", "shopping_cart data")

  Rel(customer, reactUi, "Uses", "HTTPS")
  Rel(reactUi, apiGateway, "Calls", "HTTP")
  Rel(apiGateway, products, "Calls", "HTTP, via Eureka")
  Rel(apiGateway, cart, "Calls", "HTTP, via Eureka")
  Rel(apiGateway, checkout, "Calls", "HTTP, via Eureka")
  Rel(checkout, cart, "Calls", "HTTP, via Eureka")
  Rel(checkout, products, "Calls", "HTTP, via Eureka")
  Rel(products, ycql, "Reads/Writes", "YCQL driver")
  Rel(checkout, ycql, "Reads/Writes", "YCQL driver")
  Rel(cart, ysql, "Reads/Writes", "JDBC")
  BiRel(eureka, apiGateway, "Registers/discovers")
  BiRel(eureka, products, "Registers/discovers")
  BiRel(eureka, cart, "Registers/discovers")
  BiRel(eureka, checkout, "Registers/discovers")
  BiRel(eureka, login, "Registers/discovers")
```

## C3: Component (`api-gateway-microservice` only)

```mermaid
C4Component
  title Component Diagram - api-gateway-microservice

  Container_Boundary(gw, "api-gateway-microservice") {
    Component(productController, "ProductCatalogController", "Spring MVC Controller", "Handles product catalog requests")
    Component(cartController, "ShoppingCartController", "Spring MVC Controller", "Handles cart requests")
    Component(productServiceRest, "ProductCatalogServiceRestImpl", "Spring Service", "Product catalog business logic")
    Component(cartServiceRest, "ShoppingCartServiceRestImpl", "Spring Service", "Cart business logic")
    Component(checkoutServiceRest, "CheckoutServiceRestImpl", "Spring Service", "Checkout business logic")
    Component(productClient, "ProductCatalogRestClient", "REST Client", "Calls products-microservice")
    Component(cartClient, "ShoppingCartRestClient", "REST Client", "Calls cart-microservice")
    Component(checkoutClient, "CheckoutRestClient", "REST Client", "Calls checkout-microservice")
  }

  System_Ext(productsMs, "products-microservice", "Product catalog + rankings")
  System_Ext(cartMs, "cart-microservice", "Shopping cart")
  System_Ext(checkoutMs, "checkout-microservice", "Checkout + inventory + orders")

  Rel(productController, productServiceRest, "Delegates to")
  Rel(cartController, cartServiceRest, "Delegates to")
  Rel(cartController, checkoutServiceRest, "Delegates to")
  Rel(productServiceRest, productClient, "Uses")
  Rel(cartServiceRest, cartClient, "Uses")
  Rel(checkoutServiceRest, checkoutClient, "Uses")
  Rel(productClient, productsMs, "Calls", "HTTP, via Eureka")
  Rel(cartClient, cartMs, "Calls", "HTTP, via Eureka")
  Rel(checkoutClient, checkoutMs, "Calls", "HTTP, via Eureka")
```

No C4-Code (level 4) diagram is included, per this feature's scope.
