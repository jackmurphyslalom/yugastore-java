---
description: Explicitly fold already-drafted session material into meta/rabbit-wiki/ontology.yaml and wiki entries.
---

## Notes

```text
$ARGUMENTS
```

Follow `.agents/skills/rabbit-session-close/SKILL.md` to persist only material already drafted
in this session into `meta/rabbit-wiki/ontology.yaml` and the affected `meta/rabbit-wiki/wiki/`
entries. Only run this when explicitly invoked by the user.
