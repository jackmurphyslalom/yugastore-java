---
description: Quickly convert a file or URL into a Markdown copy stored under docs/context/sources/ for future reference.
---

## Source

```text
$ARGUMENTS
```

Follow `.agents/skills/rm-archive-to-markdown/SKILL.md` to convert the source above into a stored Markdown file under `docs/context/sources/`, then report the resulting file path. Do not distill it into decisions, gaps, or other durable docs unless separately asked — that is the job of `aisdlc-knowledge-ingestion`.
